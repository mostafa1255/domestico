import 'package:flutter/material.dart';

class IotDevicesScreen extends StatelessWidget {
  static const String routeName = 'IotDevicesScreen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.orange,
      ),
    );
  }
}
