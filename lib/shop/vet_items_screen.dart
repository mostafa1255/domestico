import 'package:flutter/material.dart';

class VetItemsScreen extends StatelessWidget {
  static const String routeName = 'VetItemsScreen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.green,
      ),
    );
  }
}
